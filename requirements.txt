# uv를 사용하는 경우 pyproject.toml로 관리하세요:
#   uv sync              (의존성 설치)
#   uv sync --extra dev  (개발 의존성 포함)
#
# pip를 사용하는 경우:
#   pip install -r requirements.txt

# Agent API server
fastapi>=0.115.0
uvicorn[standard]>=0.30.0

# LangGraph
langgraph>=0.2.0
langchain-openai>=0.2.0

# Data
pydantic>=2.0.0
python-dotenv>=1.0.0
sqlalchemy>=2.0.0

# Document parsing
pypdf2>=3.0.0
python-docx>=1.1.0
httpx>=0.27.0

# Django backend
django>=5.0.0
djangorestframework>=3.15.0
psycopg2-binary>=2.9.0
